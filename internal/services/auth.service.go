package services

import (
	"myFirstGoGin/internal/models"
)

func Login(user *models.User) *models.User {
	// user.Username = "modify"
	return user
}

func Register(user *models.User) *models.User {
	// user.Username = "modify"
	return user
}
